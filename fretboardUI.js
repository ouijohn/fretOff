class Fret{
    constructor(frets, speachBubble, scoreCard, startButtons, modal)
    frets = this.frets;
    speechBubble = this.speechBubble;
    scoreCard = this.scoreCard;
    startButtons = this.startButtons;
    modal = this.modal;
}